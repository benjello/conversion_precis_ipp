# Paramètres OpenFisca

Les tableaux dynamiques (ex. évolution des taux de TVA) sont générés à partir du **paquet Python** [openfisca-france](https://github.com/openfisca/openfisca-france) installé, via son API de ressources (aucun chemin local).

```bash
uv sync
```
